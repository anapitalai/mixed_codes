apiVersion: v1
kind: Pod
metadata:
  name: emptyDir-pod
spec:
  containers:
   - image: alpine
     name: emptyDir-container
     command: ['sh','-c','sleep 3000']
     volumeMounts:
      - mountPath: /demo
        name: test-volume
  volumes:
  - name: test-volume
    emptyDir: {}